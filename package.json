{
  "name": "space-traders-console",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter web build",
    "start": "pnpm --filter web start",
    "lint": "pnpm --filter web lint",
    "generate-sdk": "cd packages/spacetraders-sdk && pnpm install"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "typescript": "^5"
  },
  "dependencies": {
    "next-themes": "^0.4.6"
  }
}
